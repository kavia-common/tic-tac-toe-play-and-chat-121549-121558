# Copy this file to `.env` in the same folder, then update the value below.
# Create React App reads env variables at build/start time; restart `npm start` after changes.
# Do NOT add a trailing slash.
#
# Example (local dev):
# REACT_APP_API_BASE_URL=http://localhost:3001
#
# Example (cloud/dev container):
# REACT_APP_API_BASE_URL=https://vscode-internal-13618-beta.beta01.cloud.kavia.ai:3001

REACT_APP_API_BASE_URL=
